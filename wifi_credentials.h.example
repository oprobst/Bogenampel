// Vorlage für WiFi-Zugangsdaten (OTA-Upload über Heimnetz).
//
// Kopiere diese Datei nach wifi_credentials.h (im selben Verzeichnis) und
// trage deine Zugangsdaten ein. wifi_credentials.h ist in .gitignore und
// wird nicht eingecheckt.
//
// Ohne diese Datei verbinden sich Sender und Empfänger als eigener SoftAP
// (SSID: "Bogenampel-Sender" / "Bogenampel-Empfaenger", PW: bogenampel).

#pragma once

#define WIFI_SSID_OVERRIDE "DeinNetzwerk"
#define WIFI_PASS_OVERRIDE "DeinPasswort"
